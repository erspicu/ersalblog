# Google Apps Script (GAS) Backend

此目錄包含部署至 Google Apps Script 的原始碼。

## 使用方式
1. 在 Google Script 編輯器中建立新專案。
2. 貼入 `Code.gs` 的內容。
3. 部署為「網頁應用程式」，並將權限設為「所有人 (Anyone)」。
4. 將取得的 URL 填回 `config/config.js`。
